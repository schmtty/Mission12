# Optional: base URL of the API when the React app is not served from the same origin
# as the backend (e.g. Azure Static Web Apps site + separate App Service API).
# Leave unset for local dev (Vite proxies /api to http://localhost:5000).
# VITE_API_BASE_URL=https://your-api.azurewebsites.net
